import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ==========================================================
  // BRAND
  // ==========================================================

  static const primary = Color(0xFF16A34A);
  static const primaryDark = Color(0xFF15803D);
  static const primaryLight = Color(0xFF22C55E);

  static const accent = Color(0xFF0F766E);

  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFDC2626);

  // ==========================================================
  // LIGHT
  // ==========================================================

  static const lightBackground = Color(0xFFFFFFFF);
  static const lightSurface = Color(0xFFF8FAFC);

  static const lightCard = Colors.white;

  static const lightText = Color(0xFF111827);
  static const lightSubtitle = Color(0xFF6B7280);

  static const lightBorder = Color(0xFFE5E7EB);

  // ==========================================================
  // DARK
  // ==========================================================

  static const darkBackground = Color(0xFF0F1115);
  static const darkSurface = Color(0xFF181A20);

  static const darkCard = Color(0xFF20232A);

  static const darkText = Color(0xFFF9FAFB);
  static const darkSubtitle = Color(0xFF9CA3AF);

  static const darkBorder = Color(0xFF2F3542);
}